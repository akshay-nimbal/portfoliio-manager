# Application
NODE_ENV=development

# Refresh interval for the dashboard polling (in milliseconds)
NEXT_PUBLIC_REFRESH_INTERVAL_MS=15000

# Server-side cache TTLs (in seconds). Yahoo refreshes more often than Google.
YAHOO_CACHE_TTL_SECONDS=15
GOOGLE_CACHE_TTL_SECONDS=900
